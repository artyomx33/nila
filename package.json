{
  "name": "nila-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:website && npm run build:app && npm run combine",
    "build:website": "cd website && npm install && npm run build",
    "build:app": "cd app && npm install && npm run build",
    "combine": "mkdir -p .vercel-output && cp -r website/.next .vercel-output/ && cp -r app/.next .vercel-output/app-next",
    "dev:website": "cd website && npm run dev",
    "dev:app": "cd app && npm run dev"
  }
}
